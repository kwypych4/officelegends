import { LoginForm } from './components';
import * as Styled from './login.styled';

export const LoginPage = () => {
  return (
    <Styled.Wrapper>
      <LoginForm />
    </Styled.Wrapper>
  );
};
