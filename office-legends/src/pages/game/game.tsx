import { GameChoose, PlaygroundMap } from '.';
import { GameWrapper } from './game.styled';

export const GamePage = () => {
  return (
    <GameWrapper>
      <PlaygroundMap />
    </GameWrapper>
  );
};
